from pathlib import Path
from typing import List

from foampostproc.model import CameraProps, CameraSlice

class ScreenshotProd:
    def make_screenshot(self, case: ICaseLike, out: Path) -> bool:
        pass
